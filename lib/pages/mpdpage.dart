import 'package:flutter/material.dart';

class MpdPage extends StatelessWidget {
  const MpdPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Mpdpage'));
  }
}
